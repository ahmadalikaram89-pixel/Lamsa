# Lamsa — TODO

## ✅ Done
- [x] Add a `README.md` explaining the app, local dev setup, and required env vars.
- [x] Remove the 3D room planner (`planner.html`, `test3d.html`, `js/three.min.js`, `js/OrbitControls.js`) and all related UI/JS in `lamsa-bilingual.html`.
- [x] Add a 2D floor plan editor in its place — draws the room from the Length/Width inputs and lets the user tap a wall to place/remove door and window markers, feeding a description into the AI prompt.
- [x] Remove the "Detect Furniture" / "Place It Yourself" module from `rearrange.html` and its `api/detect-furniture.js` endpoint, keeping only the "AI Decides" (Rearrange Only / Full Redesign) flow.
- [x] Fix regeneration crash: generating a second design in the same session silently threw on a null `#resultPlaceholder` (removed from the DOM by the first result render) and left the UI stuck on the loading spinner forever. `doGenerate()` now recreates the placeholder before each run.
- [x] Restore the "Start New Design" button's CSS, accidentally dropped when `.planner-clear-btn` was removed with the 3D planner.
- [x] Fix "Full Redesign" in Quick Rearrange producing near-identical output to the source photo — rewrote `FULL_REDESIGN_PROMPT` with mandatory, itemized change instructions (mirroring the working Rearrange Only prompt) and raised `guidance_scale` to 7.5 for that mode.
- [x] Fix the same regeneration-crash bug in `rearrange.html` (same `#resultPlaceholder` removal issue as `lamsa-bilingual.html`, found while working on the item below).
- [x] Add "Nearby Stores" — `data/furniture-stores.json` (furniture retailers by Gulf + Europe country) and `js/nearby-stores.js` (a vanilla-JS render function, since the app isn't Next.js/React) render a "Shop similar pieces" list under the generated result in both `lamsa-bilingual.html` and `rearrange.html`. Each entry is plain text (no logos) with a "View on map" link to a Google Maps search (`target="_blank" rel="noopener noreferrer"`, no paid API). Country is inferred from the currency already selected in the designer (or browser locale on `rearrange.html`, which has no currency selector). Furniture-type-based matching is stubbed for later, once Claude Vision furniture detection exists again — today it always shows the general per-country list.
- [x] Fix the 2D floor plan showing stale door/window markers from a previous, unrelated visit — it was persisting `fpItems` to `localStorage` indefinitely while every other room-info field (type, area, dimensions, style, notes...) resets on a fresh page load. It's now in-memory only for the current page load, consistent with the rest of the form.
- [x] Fix `vercel.json` not serving static files outside `/api/*` and `*.html` (no `"handle": "filesystem"` phase), which silently broke the Nearby Stores panel in production — `js/nearby-stores.js` likely 404'd, so `window.NearbyStores` stayed undefined and nothing rendered, with no visible error. Added the filesystem phase plus a `console.error` for next time.
- [x] Simplify the nav/brand logo to a single "LAMSA" wordmark (removed the Arabic "لمستي" line) on all three pages, and bumped its size up slightly.
- [x] Fix misleading "WHY LAMSA" landing page copy — two feature cards promised "Furniture List with Prices" (real IKEA/Jysk pricing) and "Direct Purchase Links" (one-click buying), neither of which was ever implemented. Reworded both to accurately describe what Nearby Stores actually does: a general store list + Google Maps search links.
- [x] Fix bathroom fixtures surviving into non-bathroom redesigns — root cause found on round 4. Rounds 1-3 (prompt wording x2, then raising `guidance_scale`) all failed; user's round-3 test still showed a shower head, now fused onto the bed frame. Round 4: looked up fal.ai's actual flux-pro/kontext API docs and found `strength` (0-1, **default 0.1**) — a parameter the app never set at all, on any of its three generation flows. `strength` caps how much the output is *allowed* to differ from the source image, completely separate from `guidance_scale` (which only controls prompt adherence); at the default 0.1 the model was barely permitted to change the photo no matter how the prompt was worded, which is why every wording/guidance_scale fix fell short. Added `strength` support to `api/generate.js` and set it per-flow on the client: `0.9` for full room strip-and-rebuild (`lamsa-bilingual.html`, `rearrange.html` Full Redesign) and `0.6` for `rearrange.html` Rearrange Only (needs to visibly move furniture while the prompt asks it to keep the furniture's appearance). **User-confirmed**: stress-tested with a genuine bathroom photo converted to a Kids Room (about as hard a case as this gets) — succeeded ~90%. **Round 5**: user re-ran the exact same photo and the floor-to-ceiling shower frame was still there, still fused onto the bed. Since there's no fixed seed, output varies run-to-run at the same settings, but `0.9` clearly wasn't reliably enough for this specific extreme source image (a full glass/metal shower enclosure spanning floor-to-ceiling — plausibly reads to the model as a "structural" element worth preserving). Raised `strength` to the maximum (`1`) for every full-room-rebuild flow, which is a real, permanent improvement (fal.ai never had this parameter set at all before this bug report — every generation across the app was defaulting to `strength: 0.1`). User decided not to keep chasing 100% reliability on this one extreme source photo (a full floor-to-ceiling glass/metal shower enclosure) — accepted as a known hard edge case rather than actively worked further.
- [x] Add Stripe payments — one-time credit purchases (no subscription), replacing the dead "Subscribe €9/mo" modal that had never actually been wired to anything. Pricing: 1 credit/€1, 5 credits/€4, 10 credits/€7 (`api/_stripe.js`). New: `package.json` (project had zero npm dependency management before this — added `stripe` + `@upstash/redis`), `.gitignore`, `api/_db.js` (Upstash Redis credit ledger — Vercel's own KV product is sunset, so this uses the Marketplace Upstash integration), `api/create-checkout-session.js`, `api/stripe-webhook.js` (signature-verified, idempotent against duplicate deliveries), `api/credits.js` (grants 1 free credit on a new email's first lookup). `api/generate.js` now requires `email` and atomically deducts 1 credit before submitting to fal.ai, refunding it if the fal.ai submission fails; returns `402` when the balance is 0. Wired a matching credit-purchase modal + balance badge into both `lamsa-bilingual.html` and `rearrange.html` (the latter had no email/identity concept at all before this, since it has no login flow — added one). Fixed a real pre-existing bug found along the way: registration on `lamsa-auth.html` collected an email field but never actually saved it to `localStorage` — only `username` was persisted. Backend logic (welcome credit, atomic deduction under concurrent load, webhook idempotency) verified against a real local Redis instance via a hand-rolled Upstash-REST-protocol shim, since no real Upstash/Stripe credentials exist in this environment — **still needs end-to-end confirmation with real Stripe/Upstash accounts once the user sets those up** (see README's "One-time setup required" section).
- [x] Build real server-side login, replacing the fake client-side-only auth (this closes the "credits keyed by an unverified email" gap from the Stripe work above — anyone could previously type any email into the credits modal and spend/purchase on that identity). New `api/_auth.js`: password hashing via Node's built-in `crypto.scrypt` (random per-user salt, `crypto.timingSafeEqual` comparison — no new dependency needed), opaque random session tokens stored in Redis (`lamsa:session:<token> → email`, 30-day TTL) delivered as an `httpOnly; SameSite=Lax` cookie (`Secure` added outside `localhost`). New endpoints `api/auth-register.js` (creates the account, grants the welcome credit immediately instead of lazily on first `/api/credits` call, starts a session), `api/auth-login.js` (single generic "Invalid email or password" error for both unknown-email and wrong-password cases, to prevent user enumeration), `api/auth-logout.js`, `api/me.js` (session → `{username, email}`, used by every protected page's auth guard). **The actual security fix**: `api/generate.js`, `api/credits.js`, and `api/create-checkout-session.js` now derive the user's identity *only* from the verified session cookie (`requireSessionEmail()`) — the old client-supplied `email` request field is gone entirely, so it's no longer possible to spend or charge someone else's credits by typing their email. `lamsa-auth.html` rewritten to call the real endpoints instead of writing straight to `localStorage`; the Google/Apple sign-in buttons now honestly say "coming soon" instead of faking a logged-in state. `lamsa-bilingual.html` and `rearrange.html` (the latter had no auth guard at all before this) both gained a `GET /api/me` guard on load plus a Log Out button, and had the credits-modal email input field removed since identity now comes from the session, not user-typed text. Verified end-to-end (not mocked) against a real local Redis + a minimal Vercel-mimicking dev server that runs the actual `api/*.js` handlers: register → session cookie set (`httpOnly`, verified) → protected pages load → welcome credit visible → credits-modal email field confirmed absent → logout destroys the session and re-locks protected pages → login with correct/incorrect password both behave correctly. Also fixed, while in this code: the `api/generate.js` double-refund edge case flagged below (refund call now wrapped in its own try/catch so a failure inside it can't cascade into the outer catch's refund).
- [x] Fix "Continue with Apple" button getting cut off on the mobile auth page — on screens ≤700px, `body` stayed locked at `height: 100vh` while the login/register form panel was a `flex:1` child with its own `overflow-y: auto`, splitting one viewport height between the hero and the form. The form got squeezed into whatever sliver was left under the hero, so only its very top edge was visible before hitting its own cramped internal scroll boundary. Now the body grows to its natural content height and the whole page scrolls normally.
- [x] Add a real app icon/logo, replacing the placeholder "✦" sparkle emoji used everywhere (browser tab, phone home-screen bookmark, and the in-page nav mark all had no real branding — iOS was auto-generating a plain black-square-with-"L" icon since no favicon/apple-touch-icon existed at all). Designed a gold "L" monogram with a small diamond accent (matches the existing black/gold palette and the "touch" sparkle motif) as `assets/logo-mark.svg` (icon-only, drops into the existing bordered nav square on all three pages) and `assets/favicon.svg` (same mark with its own dark rounded-square background, for the browser tab / home-screen icon), plus PNG fallbacks (`favicon-32.png`, `favicon-16.png`, `apple-touch-icon.png`, 180×180 for iOS) rendered via headless Chromium since no SVG-to-PNG tool was available in this environment. Wired `<link rel="icon">` / `<link rel="apple-touch-icon">` into all four HTML pages and swapped the in-page `✦` for the real mark in `lamsa-auth.html`, `lamsa-bilingual.html`, and `rearrange.html`.
- [x] Replace the placeholder "L" monogram with the user's actual designed logo (a gold Celtic-knot shield emblem, designed externally on Gemini and supplied as a full marketing image with a fabric-texture background). Since it wasn't a clean icon asset, cut out just the shield: built a luminance-based alpha mask (gold emblem vs. dark textured background — clean histogram separation around lum≈95) with `scipy.ndimage` connected-component filtering to drop small fabric-texture speckles while keeping every real stroke of the knotwork (the strands aren't 4-connected to each other at the pixel level, so a naive "largest component" approach cut the design apart — fixed by keeping all components above a size threshold instead of only the largest one). No image-matting tool (`rembg`) was usable — its model download from a GitHub release asset is blocked by this environment's egress policy — so this was done manually with PIL/numpy. Output: `assets/logo-mark.png` (transparent cutout, used in the nav) and regenerated `favicon-32.png` / `favicon-16.png` / `apple-touch-icon.png` (shield composited onto the existing dark rounded-square + gold border style). Removed the now-unused `favicon.svg` / `logo-mark.svg` placeholder files and updated the icon `<link>` tags (PNG-only now, no SVG source exists for the real logo). Verified by screenshotting the nav logo on all three pages and the icon at actual favicon/apple-touch-icon sizes.
- [x] Fix the register-form terms text (`"By registering, you agree to Terms of Service and Privacy Policy"`) staying in Arabic no matter which language was selected — e.g. switching to German translated every other label and button correctly but left this one line in Arabic. Root cause: the translations for it (`terms`, `termsLink`, `andText`, `privacyLink`) already existed for all 8 languages in the `translations` object, but the `<p class="terms-text">` in the HTML was hardcoded Arabic text with no element `setLang()` could target, so the translation data was simply never applied to it. Fixed by wrapping the text/links in `#termsText`/`#termsLink`/`#andText`/`#privacyLink` spans and wiring them into `setLang()`. Verified with Playwright across German, Arabic, and French — all now show fully in the selected language, no leftover Arabic. This was found live in production while walking the user through the Upstash Redis setup (see the auth-registration debugging session above) — unrelated to that issue, but reported by the user in the same session.
- [x] Remove the redundant "لمستي" from the footer brand mark on `lamsa-bilingual.html` — `<div class="footer-logo">` was hardcoded `لمستي ✦ Lamsa` regardless of language, the same duplication issue already fixed on the nav logo earlier in this session but missed in the footer. Now just `✦ Lamsa`.
- [x] Two bugs on `lamsa-auth.html`, reported by the user testing on their phone (German UI): (1) the hero subtitle claimed the app supports "vier Sprachen" (four languages) in German — and the same mistake existed in the Arabic version too ("بأربع لغات") — when the app actually supports all 8; every other language's copy correctly said "8". Fixed both, including the raw HTML fallback text that briefly shows before `setLang('en')` runs on load. (2) The post-registration/login success screen ("Welcome to Lamsa! Start Designing") had no way to dismiss it besides clicking through to the app — no close button existed at all. Added a small `×` button (`.success-close`, top corner, mirrors correctly in RTL) that hides the success box and restores the login/register form underneath.
- [x] Fix the show/hide-password eye button overlapping the typed password on `lamsa-auth.html` — `.toggle-pass` was absolutely positioned at `left: 14px` but the password `<input>` had no matching left padding reserved for it (only `padding-right` for the lock icon on the other side), so the button sat directly on top of the first few characters/dots. Also switched `.input-icon`/`.toggle-pass`/the input's padding from physical `left`/`right` to logical `inset-inline-start`/`inset-inline-end`/`padding-inline-*`, since the hardcoded physical values would have put the icons on the wrong sides once mirrored for Arabic (RTL) — verified both English and Arabic now render correctly with no overlap.
- [x] Fix "can't find Austria" in the registration country picker — Austria was actually already in the list (`🇦🇹 النمسا`), but `filterCountries()` only matched against the Arabic `name` field (plus a near-useless reversed code check), so typing "Austria" in English — the natural thing to do with the page set to English — returned nothing. Added an `nameEn` (and `alias` for a few countries with common alternate names, e.g. UAE/USA/UK) field to every country and search now matches Arabic name, English name, alias, or exact country code. Also fixed a related, more fundamental issue found while fixing this: the country grid always displayed Arabic names regardless of the selected UI language, because `renderCountries()` hardcoded `c.name`; now it shows `c.nameEn` for every non-Arabic language and re-renders whenever the language changes (previously `setLang()` never touched the grid at all, so even after this fix the cards would have kept showing stale text until the user retyped in the search box). While wiring `renderCountries` to read `currentLang`, caught a real regression before it shipped: `currentLang` was declared with `let` far below its first read (the initial `renderCountries(countries)` call on page load), which would have thrown a temporal-dead-zone `ReferenceError` and silently broken the entire script on every page load (including language switching, login, and registration) — moved the declaration to the top of the script, verified no console errors on load.

- [x] Migrate accounts, sessions, and the credit ledger from the previous custom scrypt+Redis setup to Supabase — replacing Upstash Redis (`@upstash/redis` removed from `package.json`, `@supabase/supabase-js` added). New `api/_supabase.js` (service-role `supabaseAdmin` + anon `supabaseAnon` clients). `api/_auth.js` rewritten: registration goes through `supabase.auth.admin.createUser()` (service role, `email_confirm: true`, no separate verification step), login through `supabase.auth.signInWithPassword()` (anon key) — no password hash is ever stored or handled by this codebase anymore. Sessions are now Supabase's own JWT access token + refresh token, delivered as two `httpOnly` cookies (`lamsa_at` / `lamsa_rt`, replacing the old single `lamsa_session` cookie); `requireSessionUser()` validates the access token via `supabase.auth.getUser(jwt)` and, if expired, transparently refreshes it via `supabase.auth.refreshSession({ refresh_token })` and re-issues both cookies — callers (`api/generate.js`, `api/credits.js`, `api/create-checkout-session.js`, `api/me.js`) never see the expiry/refresh logic. Logout now also revokes the session's refresh token server-side (`supabase.auth.admin.signOut(jwt, 'global')`), not just clearing cookies. Credits moved from a Redis key-value ledger to a Postgres `credits` table on Supabase (`sql/schema.sql` — run once in the Supabase SQL Editor, not automated by this codebase, same pattern as the Stripe one-time setup); the atomic Lua `DECRBY`-if-positive script became a Postgres `deduct_credit()` function (`UPDATE ... WHERE balance > 0 RETURNING`, equally atomic under Postgres row locking), plus `add_credits()` and `ensure_welcome_credit()`. `api/_db.js` keeps the exact same exported function signatures (`getCredits`, `ensureWelcomeCredit`, `addCredits`, `deductCredit`, `markEventProcessed`) so `api/generate.js`, `api/credits.js`, and `api/stripe-webhook.js` needed only import-path changes, not logic changes. README's Authentication/Credits/env-vars/setup sections updated to match. Verified `node --check` on every touched `api/*.js` file and a clean `npm install` (Supabase client API — `getUser(jwt)`, `refreshSession({refresh_token})`, `admin.createUser`, `admin.signOut` — confirmed against the installed `@supabase/auth-js` source directly, since `supabase.com` docs were unreachable from this environment's egress proxy). **Not yet done, now unblocked by this migration**: Google/Apple OAuth and password reset are both natively supported by Supabase Auth and just need wiring into `lamsa-auth.html` (see below) — no new backend work required. **Still needs end-to-end confirmation with a real Supabase project** (no live credentials in this environment); the SQL schema also needs to actually be run once via the Supabase SQL Editor before this works in production.
- [x] Recolor the real Lamsa logo (the gold Celtic-knot shield emblem) to silver, per the user's request — **not** the interim "L" monogram placeholder from an earlier session, which a previous attempt in this session mistakenly restored instead without actually looking at what the shield emblem — the user's actual designed logo — looked like; reverted that mistake and redid it against the real asset. Since the shield is a raster cutout with no vector source (originally cut from an external marketing image, per the `5bdb7e6` commit), recoloring meant per-pixel luminance remapping with PIL/numpy: computed each shield pixel's luminance and re-mapped it through a silver gradient (dark steel `#1a1b1f` shadows through bright `#f7f9fb` highlights) instead of the original gold ramp, preserving alpha and every stroke of the knotwork shading exactly — installed `pillow`/`numpy` via pip for this, not present in the environment by default. Regenerated `assets/logo-mark.png` (transparent silver cutout, used in the nav) and recomposited `assets/favicon-32.png` / `favicon-16.png` / `apple-touch-icon.png` (silver shield centered over the same dark rounded-square + **gold** border template as before — the border/frame intentionally stays gold, matching the nav's `.brand-icon` border elsewhere, only the emblem itself is silver) via headless Chromium. Removed the `assets/logo-mark.svg` / `favicon.svg` files added by the earlier mistaken attempt (no vector source exists for the real logo, same as the original `5bdb7e6` reasoning). Verified by screenshotting the rendered nav on `lamsa-auth.html`.

## 💡 Suggestions
_(لسا ما في شي)_

## Auth & security
- [ ] Add a password reset flow (`forgotPass` string exists in translations but has no handler; Supabase Auth already supports this via `resetPasswordForEmail` — just needs a UI + endpoint).
- [ ] Wire up real Google/Apple OAuth (currently just shows "coming soon"; Supabase Auth supports both natively — enable the provider in the Supabase dashboard and add `supabase.auth.signInWithOAuth()` on the client).
- [ ] Rate-limit `api/generate.js`, `api/upload.js`, `api/status.js`, `api/credits.js`, `api/create-checkout-session.js`, `api/auth-login.js`, and `api/auth-register.js` — nothing currently stops repeated calls from burning FAL credits, spamming Stripe Checkout session creation, or brute-forcing a login.

## API / backend
- [ ] Add request body validation (size limits, allowed `mime_type`, `aspect_ratio` enum) to `api/generate.js` and `api/upload.js` before forwarding to fal.ai.
- [ ] Centralize the fal.ai queue polling logic shared by `api/generate.js` and `api/status.js` instead of duplicating the base URL/queue handling.
- [ ] Replace `console.log`/`console.error` debug statements in `api/*.js` with a proper logger (or strip them for production).
- [ ] Delete the stray duplicate `upload.js` at the repo root — it's an unused copy of `api/upload.js` (only `api/` is routed per `vercel.json`).

## Project hygiene
- [ ] Add basic tests for the `api/*.js` handlers (mock fal.ai/Stripe/Upstash responses, verify status codes and error paths) — everything so far has been validated manually per-change (Playwright for the frontend, a hand-rolled Redis shim for the credit logic), not as a repeatable suite.

## Frontend
- [ ] Consolidate the growing pile of inline "BUG N fix" comments in `lamsa-bilingual.html` (scroll-lock, panel reset) into named, testable functions instead of ad-hoc patches.
- [ ] `lamsa-bilingual.html` is ~160KB of inline HTML/CSS/JS — consider splitting into separate JS/CSS files for maintainability.
- [ ] Add the 2D floor plan's door/window strings to the non-English/Arabic translation dictionaries in `lamsa-bilingual.html` (they currently fall back to English).
- [ ] Remove the now-orphaned `'Furniture List with Prices'` / `'Direct Purchase Links'` entries left behind in the 7 non-English/Arabic translation dictionaries after the WHY LAMSA copy fix — dead code, harmless but unused. Also retranslate the two reworded cards into those languages (currently fall back to English).
- [ ] The credits/checkout modal (`showCreditsModal()` in both `lamsa-bilingual.html` and `rearrange.html`) only has English and Arabic text — falls back to English for the other 5 supported languages, same pattern as the floor plan and Nearby Stores features above.

## Product
_(لسا ما في شي)_
